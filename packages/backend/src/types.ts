import type { SDK } from "caido:plugin";

export type BackendSDK = SDK;

export type {
  AuthConfig,
  AuthenticationInfo,
  EnvironmentCreationResult,
  EnvironmentVariable,
  ImportResult,
  Result,
  SessionCreationResult,
} from "shared";
